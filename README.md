# 轮回女祭司

GitHub Pages 托管文件。

发布方式：

1. 在 GitHub 创建一个新公开仓库，例如 `birthday-script`.
2. 上传本目录里的 `index.html`。
3. 进入仓库 `Settings` -> `Pages`。
4. Source 选择 `Deploy from a branch`。
5. Branch 选择 `main`，目录选择 `/root`。
6. 保存后等待 1-3 分钟，页面地址通常是：

`https://你的用户名.github.io/birthday-script/`

